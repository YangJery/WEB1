# 필라테스 동작 사전

필라테스 매트 동작을 글·사진·영상으로 정리해 공유하는 **정적 웹사이트**입니다.
저장소 주인 한 사람이 글을 올리고, 방문자는 읽기만 합니다.

- 공개 주소: https://yangjery.github.io/WEB1/
- 관리 화면: https://yangjery.github.io/WEB1/admin.html
- 저장소: github.com/YangJery/WEB1 (공개)

## 구조

빌드 과정이 없습니다. 브라우저가 파일을 그대로 읽습니다.

```
index.html       동작 목록. js/list.js 가 카테고리별 카드로 그림
movement.html    동작 상세. ?id=hundred 형태로 하나를 지정
admin.html       관리 화면. 주인만 쓰는 글쓰기 폼
css/style.css    사이트 스타일
css/admin.css    관리 화면 스타일
js/data.js       ★ 모든 동작 내용. CATEGORIES 와 MOVEMENTS 두 배열
js/common.js     목록·상세 공용 (난이도 배지, 유튜브 주소 해석)
js/bodymap.js    주요 사용 근육을 인체 도식에 칠해 보여줌 (상세 전용)
js/hero.js       첫 화면에서 동작을 순회하며 근육을 켬 (홈 전용)
js/reveal.js     스크롤에 따라 나타나기 + 상단 막대
js/list.js       목록 그리기
js/detail.js     상세 그리기
js/github.js     GitHub API 로 저장소에 읽고 쓰기 (관리 화면 전용)
js/admin.js      관리 화면 동작
images/          동작 사진
```

## 알아둘 것

**데이터는 `js/data.js` 한 곳에 있습니다.** 관리 화면이 이 파일을 통째로 다시
만들어 냅니다. 손으로 고칠 때도 형식을 지켜야 하고, 깨지면 목록이 통째로 빈 화면이
됩니다. `JSON.stringify` 로 생성되므로 주석이나 손으로 넣은 서식은 다음 발행 때
사라집니다.

**정적 파일만 있습니다.** 서버도 빌드도 없습니다. `index.html` 을 브라우저로 바로
열어도 동작해야 합니다 — 그래서 데이터를 JSON + fetch 가 아니라 `<script>` 로
불러오는 `data.js` 에 둡니다. 이 성질을 깨는 변경은 하지 마세요.

**배포는 `main` 에 올라가면 자동입니다.** GitHub Pages 가 40초쯤 걸려 반영합니다.
브랜치에만 있으면 사이트에 나타나지 않습니다.

**관리 화면의 토큰.** GitHub 파인그레인드 토큰을 브라우저 localStorage 에 30일 기한으로
둡니다. GitHub Pages 는 계정의 모든 프로젝트 사이트를 `yangjery.github.io` 하나의
주소에서 내려주고 저장 공간은 그 주소까지로만 구분되므로, **그 주소에서 실행되는
스크립트는 무엇이든 토큰을 읽을 수 있습니다.** 사진 업로드를 `jpg/png/webp/gif` 로
제한하는 이유가 이것입니다(`svg` 는 스크립트를 담을 수 있음). 이 제한을 풀지 마세요.

**인체 도식.** `js/bodymap.js` 가 동작의 `targets` 값을 `MUSCLE_REGIONS` 표로 찾아
해당 부위를 칠합니다. **동작을 추가할 때 근육 이름만 적으면 그림은 자동으로 붙습니다.**
표에 없는 이름을 쓰면 그 이름만 그림에서 빠지므로, 자주 쓰게 되면 표에 한 줄
추가하세요. 지금 65개 이름과 16개 부위가 들어 있습니다.

조회는 문자열 완전 일치입니다. 그래서 **`BODY_REGIONS` 의 `label` 은 반드시
`MUSCLE_REGIONS` 의 키로도 있어야 합니다** — 범례에 보이는 이름을 그대로 적었는데
그림에 안 나타나는 일이 생기기 때문입니다. 부위를 추가하면 라벨도 함께 넣으세요. 대응이 없는 이름(`척추 분절` 같은 근육이 아닌 항목)은 그림에는 빠지고
아래 태그 목록에만 남습니다. 칠할 부위가 하나도 없으면 그림 자체를 만들지 않습니다.

직접 그린 도식이며 정밀한 해부도가 아닙니다. 외부 해부학 이미지를 가져다 쓰지
마세요 — 대부분 저작권이 있습니다.

사진이 없는 사이트라 **이 도식이 사진 자리를 대신합니다.** 첫 화면에서는 동작을
순회하며 근육이 켜지고, 목록 카드에도 그 동작의 도식이 들어갑니다.

**나타나기 효과는 반드시 실패에 안전해야 합니다.** 기본 상태는 '보임' 이고,
`js/reveal.js` 가 `js-reveal` 을 붙인 뒤에야 숨겼다 보여줍니다. 거기에 2.5초 뒤
전부 드러내는 안전망까지 둡니다. 내용이 보이지 않는 것보다 효과가 없는 편이
낫습니다. 이 구조를 단순한 `opacity: 0` 으로 되돌리지 마세요.

**CSS 에서 `ch` 단위를 큰 글씨를 감싼 상자에 쓰지 마세요.** `ch` 는 그 요소의
글자 크기로 계산되므로, 본문 크기 상자에 `max-width: 22ch` 를 주면 그 안의 56px
제목이 190px 폭에 갇혀 토막납니다. 폭은 px 로 적으세요.

**한글은 `word-break: keep-all`** 이 필요합니다. 없으면 단어 중간에서 줄이 바뀝니다.

## 확인 방법

브라우저가 컨테이너에 설치돼 있어 실제 렌더링을 볼 수 있습니다.

```bash
CHROME=$(find /opt/pw-browsers -name headless_shell -type f | head -1)
"$CHROME" --headless --no-sandbox --disable-gpu --hide-scrollbars \
  --window-size=1100,1600 --screenshot=/tmp/shot.png \
  "file:///home/user/WEB1/index.html"
```

`--dump-dom` 으로 만들어진 HTML 을, `--virtual-time-budget=2000` 으로 스크립트가
끝난 뒤 상태를 볼 수 있습니다.

데이터 형식 검사는 node 로 합니다.

```bash
node -e "$(cat js/data.js); console.log(MOVEMENTS.length, CATEGORIES)"
```

## 글쓰기 규칙

화면에 나오는 글은 모두 한국어입니다. 동작 이름은 한글 표기와 영문을 함께 둡니다
(헌드레드 / The Hundred). 운동 정보이므로 **단정적인 의학적 주장을 하지 않고**,
통증 시 중단과 전문가 상담 안내를 유지합니다.

커밋 메시지와 PR 본문은 영어로 씁니다. 사용자와의 대화는 한국어입니다.

## 담당(서브에이전트)

`.claude/agents/` 에 역할별 담당이 있습니다. `@agent-<이름>` 으로 부릅니다.

| 이름 | 하는 일 | 파일을 고치나 |
|---|---|---|
| `product-manager` | 흐릿한 요청을 범위와 작업 목록으로 | 아니오 |
| `frontend` | 화면에서 도는 코드, 새 기능, 브라우저 버그 | 예 |
| `backend` | 데이터 구조, GitHub API 연동, 저장·충돌 처리 | 예 |
| `designer` | 레이아웃·색·여백, 휴대폰에서의 모양 | 예 |
| `accessibility` | 낭독기, 키보드, 색 대비, 글자 크기 | 예 |
| `qa` | 실제 브라우저로 띄워 확인 | 아니오 |
| `content-reviewer` | 동작 설명의 안전성·정확성 | 아니오 |
| `translator` | 한국어 ↔ 영어, 필라테스 용어 표기 | 예 |
| `marketing` | 검색, 링크 미리보기, 무엇부터 채울지 | 예 |
| `site-ops` | 배포 상태, 브랜치·PR, 페이지 무게 | 아니오 |

이 프로젝트에는 서버가 없으므로 `backend` 는 데이터 계층과 GitHub API 연동을
맡고, 서버가 정말 필요해지는지 판단하는 역할도 겸합니다.

## 일 흐름

요청 종류마다 순서가 있습니다. 담당이 자기 차례를 끝내면 **다음 담당에게 넘어갑니다.**
사용자가 "이제 QA 해줘"라고 말해줄 때까지 기다리지 않습니다.

| 요청 | 순서 |
|---|---|
| 새 동작 추가 | 형식 확인 → 초안 → `content-reviewer` → `accessibility`(alt) → **사용자 발행** → `qa` |
| 기존 동작 수정 | 대상 확인 → 수정안 → `content-reviewer` → **사용자 발행** → `qa` |
| 화면·디자인 변경 | `designer` → `frontend`(필요시) → `accessibility` → `qa` |
| 기능 추가·버그 | `product-manager`(범위) → `frontend` → `qa` |
| 데이터 구조·발행 오류 | `backend` → `qa` |
| 검색·링크 미리보기 | `marketing` → `qa` |
| 반영 안 됨·배포 문제 | `site-ops` → `qa` |

- **`qa` 는 마지막에 항상 붙습니다.** 고친 담당이 스스로 "됐다"고 끝내지 않습니다.
- `qa` 가 문제를 찾으면 **고친 담당에게 돌아갑니다.** `qa` 는 고치지 않습니다.
- 한 명으로 끝나는 일이면 그 한 명만 부릅니다. 순서를 형식적으로 다 거치지 마세요.
- 자주 하는 일은 커맨드로 있습니다: `/new-movement` · `/edit-movement` · `/deploy-check`

## 결정 권한

**반드시 사용자에게 물어야 하는 것 — 아래 일곱 개뿐입니다.**

1. `index.html` 을 브라우저로 바로 열어도 도는 전제를 깨는 변경 (`fetch`, 빌드 과정, 서버)
2. npm 패키지 · CDN 라이브러리 · 외부 폰트 · 프레임워크 도입
3. `js/data.js` 형식 변경 (= `js/admin.js` 의 `buildDataFile()` 동시 변경)
4. 사이트 전체의 색·톤 방향 변경 (아래 디자인 규칙 2번)
5. 사진 업로드 확장자 제한 완화 · 추적 스크립트 · 광고 도입
6. `main` 직접 push · force push · 브랜치나 파일 대량 삭제
7. 실제 발행 (관리 화면에서 누르는 것)

**그 외에는 담당이 정하고, 왜 그렇게 했는지 근거를 보고에 남깁니다.**

되돌리기 쉬운 판단을 매번 묻지 마세요. 여백 값, 문구 다듬기, alt 텍스트,
버그 수정 방법, 파일 정리, 검토 의견 — 전부 담당이 결정합니다.

판단이 서지 않을 때는 묻는 대신 **두 안을 만들어 차이를 보여주고 추천을 답니다.**
"어떻게 할까요?" 보다 "A와 B가 있고 B를 권합니다, 이유는…" 가 낫습니다.

## 디자인 규칙 (반드시 준수)

방향은 2026-09-12 사용자 결정으로 **"큰 타이포 + 밝고 어두운 구역 교차"** 로
바뀌었습니다. 그 전의 "따뜻한 아이보리 + 세이지 그린, 절제된 대비" 는 더 이상
유효하지 않습니다. 이 방향을 다시 바꾸는 것도 사용자 결정 사항입니다(결정 권한 4번).

1. **색은 `css/style.css` 의 `:root` 변수로만 씁니다.** 하드코딩 금지.
   새 변수를 만들기 전에 기존 것으로 되는지 먼저 봅니다. 현재 확정값:

   | 변수 | 값 | |
   |---|---|---|
   | `--paper` | `#fbfaf7` | 기본 배경. 어두운 구역의 본문 색도 이것 |
   | `--surface` | `#ffffff` | 카드·표면 |
   | `--night` | `#12120e` | 어두운 구역 배경 |
   | `--ink` | `#16150f` | 본문 |
   | `--ink-soft` | `#5f5c52` | 보조 텍스트 (밝은 구역) |
   | `--ink-dim` | `#a5a198` | 보조 텍스트 (어두운 구역) |
   | `--ink-hover` | `#2a2820` | 검은 버튼 hover |
   | `--line` | `#e6e2d9` | 테두리 |
   | `--accent` | `#4f6b47` | 밝은 구역의 강조색 |
   | `--accent-bright` | `#8ab87c` | 어두운 구역의 강조색 |
   | `--caution` | `#b06a6a` | 주의 항목 불릿 |
   | `--figure` | `#cfcaba` | 인체 실루엣 (밝은 구역) |
   | `--figure-dark` | `#4a483c` | 인체 실루엣 (어두운 구역) |
   | `--media-bg` | `#14130f` | 영상 자리 바탕 |
   | `--on-dark` | `#ffffff` | 어두운 표면 위의 글자 |
   | `--topbar-bg` | `rgba(251,250,247,.85)` | 상단바. 알파를 내리면 링크 대비가 깨짐 |
   | `--overlay-bg` | `rgba(18,18,14,.74)` | 카드 위 배지 |
   | `--bg` `--text` `--muted` `--border` `--radius` `--accent-soft` | 위 값들의 별칭 | `css/admin.css` 전용. 비우면 관리 화면 테두리가 사라짐 |
   | `--thumb-from` / `--thumb-to` | `#f1efe8` / `#e6e9e2` | 카드 그림 바탕 기울기 |
   | `--level-*-bg` / `--level-*-fg` | 아래 3번 | 난이도 배지 6개 |
   | `--shell` | `1240px` | 본문 최대 폭 |
   | `--ease` | `cubic-bezier(0.22, 1, 0.36, 1)` | 모든 전환에 같은 곡선 |

   실루엣 색(`--figure`, `--figure-dark`)은 **두 방향으로 동시에 묶여 있습니다.**
   배경과 구분돼야 하고, 켜진 근육과도 3:1 이상 벌어져야 합니다. 실루엣을 더
   진하게 하면 켜진 근육이 묻힙니다. 지금 값은 배경 대비 1.57 / 2.04, 켜진 근육
   대비 3.63 / 4.04 입니다. 바꾸려면 양쪽을 함께 재계산하세요.

2. **어두운 구역(`.band-dark`)은 강조하고 싶은 한 곳에만 씁니다.** 지금은 상세
   페이지의 "주요 사용 근육" 한 구역뿐이고, 홈에는 없습니다. 여러 곳에 흩뿌리면
   강조가 사라집니다.
   어두운 바탕에서는 강조색이 `--accent-bright`, 보조 텍스트가 `--ink-dim`,
   실루엣이 `--figure-dark` 로 바뀝니다. 밝은 구역용 색을 그대로 쓰면 읽히지
   않습니다. `.band-dark` 안에서 색을 새로 쓸 때마다 대비를 재세요.
3. **난이도 배지 색(초급·중급·고급)은 의미를 가진 색입니다.** 임의로 바꾸지 않습니다.
   `--level-beginner-bg/fg` `#e4ede0`/`#3d5c36`,
   `--level-intermediate-bg/fg` `#f6ebd8`/`#7d541c`,
   `--level-advanced-bg/fg` `#f5e2e2`/`#8a3838`. 명암비 6.29 / 5.64 / 6.24.
4. 컨테이너 `--shell`(1240px) · 사진 4:3 · 모바일은 390px 기준 · 터치 영역 44px 안팎.
5. CSS 파일은 `style.css` 와 `admin.css` 두 개를 넘기지 않습니다.
6. **글자 크기는 `clamp()` 로 화면 폭에 따라 변합니다.** 고정 px 로 되돌리지 마세요.
   제목 `clamp(46px, 7.4vw, 92px)` · 구역 제목 `clamp(30px, 4.4vw, 56px)`.
7. **`padding` 을 한 줄로 덮어쓰지 마세요.** `.shell` 의 좌우 여백까지 사라집니다.
   반응형에서 위아래만 고칠 때는 `padding-top` / `padding-bottom` 을 씁니다.

### 다른 사이트를 참고하라는 요청이 왔을 때

"애플 홈페이지처럼", "메타 사이트 참고해서" 같은 요청은 **그대로 따르지 마세요.**
그 사이트들을 실제로 볼 수 없고, 참고 대상끼리도 방향이 서로 반대인 경우가 많습니다.
사용자가 말한 것은 디자인이 아니라 **인상**입니다. 인상을 원칙으로 번역해 확인합니다.

1. 구체적으로 무엇을 원하는지 되묻습니다 —
   여백을 더 크게? 사진을 더 크게? 글자 위계를 단순하게? 첫 화면을 더 비우게?
2. 그것을 **수치로 바꿔 제안합니다.** "히어로 여백 64px → 96px", "제목 34px → 44px",
   "글자 크기 단계를 10개에서 5개로" 처럼 확인 가능한 형태로.
3. 위 1~7번 규칙과 부딪히면 **부딪힌다고 말하고**, 규칙을 바꿀지 먼저 확인합니다.

**참고 사이트의 색·레이아웃을 그대로 옮겨오지 않습니다.**
사용자가 캡처를 보내주면 그때는 실제로 보고 맞출 수 있습니다.
